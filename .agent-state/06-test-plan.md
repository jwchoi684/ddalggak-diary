# Test Plan — REQ-019

## backup.test.ts (17 cases)
BU1-BU6 cover buildBackup, validateBackup (valid/invalid JSON/wrong version), applyBackup (overwrite/merge with existing-wins).

## settings/page.test.tsx (5 cases)
SP1-SP3: export button, import file select + validate fail, import success + apply.

## Verdict
PASS
