# REQ-005: 디자인 시스템 프리미티브

## Status
DONE

## Completion Notes
첫 cycle에서 13개 phase 모두 PASS. Fix cycle 없음. 가장 큰 REQ였음 — 17개 신규 파일(7 컴포넌트 + 2 훅 + 8 테스트). 디자인 시스템 폴더가 MoodIcon 1개에서 9개 primitive로 확장.

`useDialogControl` 훅을 BottomSheet/ConfirmDialog가 공유 → CLAUDE.md UI 컴포넌트 재사용 규칙(항목 3 "유사하면 일반화") 실천. 100줄 룰 자연스럽게 지켜짐.

새 토큰 `--shadow-card`, `--color-danger: #C53030` (WCAG AA 5.4:1) 추가 + `dialog::backdrop` 글로벌 규칙. Tailwind 4 `bg-danger` 유틸 자동 생성 확인됨.

## Files Changed
- 신규(source, 9): `src/design-system/{Card.tsx (34L), EmptyState.tsx (49L), IconButton.tsx (46L), FAB.tsx (35L), useDialogControl.ts (45L), BottomSheet.tsx (54L), Toast.tsx (48L), ConfirmDialog.tsx (79L), useToast.ts (57L)}` — 모두 ≤ 100 lines
- 신규(test, 8): `src/design-system/__tests__/{Card, EmptyState, IconButton, FAB, useDialogControl, BottomSheet, Toast, ConfirmDialog, useToast}.test.{tsx,ts}` (49 cases)
- 수정: `src/app/globals.css` (+10 lines additive: `--shadow-card`, `--color-danger`, `dialog::backdrop`)
- 변경 없음: `package.json` (devDeps 추가 없음)

## Tests Run
- `npm run typecheck` — PASS
- `npm run lint` — PASS  
- `npm test` — PASS (131/131, 19 files: 82 baseline + 49 REQ-005)
- `npm run build` — PASS (Next 15.5.18, `bg-danger` Tailwind utility auto-generated)
- `scripts/quality-gate.sh` — `QUALITY_GATE_PASS`

## Review Result
PASS — `.agent-state/09-code-review-report.md`. 4개 non-blocking 제안. 모든 컴포넌트 ≤ 100 lines, contract verbatim 일치, 토큰 only 스타일링, no `as any`.

## Security Result
PASS — `.agent-state/10-security-report.md`. 0 new issues. XSS/injection grep 통과. `<dialog showModal()>`는 click-jacking 방어로 positive. New deps 0건, audit delta 0.

## E2E Result
PASS — not applicable. Primitives는 아직 page에서 mount되지 않음. 첫 브라우저 E2E는 REQ-007 (Playwright 부트스트랩 동시).

## Commit / PR
Not committed. 다음 REQ-005 단독 commit 권장 (사용자 결정).

## Forward Constraints
- **REQ-007** (calendar, 31 cells):
  - 셀 단위 React.memo + useCallback로 콜백 stabilize (IconButton 자체에는 memo 적용 금지).
  - Playwright + 첫 브라우저 E2E 부트스트랩.
  - 디자인 시스템 primitive 재사용 검증 (헤더 IconButton 3개, FAB, Card).
- **REQ-008** (mood picker, 첫 BottomSheet consumer):
  - BottomSheet always-mounted 원칙 — 화면당 5개 초과 금지.
  - 여러 sheet 필요 시 단일 BottomSheet + swappable children 패턴.
- **REQ-009 이후 dialog 다중 mount 시**:
  - ConfirmDialog의 `id="confirm-msg"` → `useId()`로 마이그레이션 (현재 단일 mount만 가정).
- **REQ-016** (persona picker): same always-mounted 제약.
- **`onClose`/`durationMs` 미사용 in Toast body**: 다음 REQ에서 Toast 사용 시, `useToast()` 훅과 함께 사용해야 정상 동작 (Toast는 pure controlled).
- (Carry-forward) REQ-011 / REQ-019의 hard-gate 유지.

## Source
docs/requirements.md §1.6 (디자인 언어 전체), §5 (공통 컴포넌트), §1.6.6 (컴포넌트 스타일 테이블)

## Summary
PRD 전반에서 반복되는 시각 패턴 — 흰 원형 헤더 버튼, 검정 FAB, 흰 카드, 바텀시트, 알약형 토스트, 확인 다이얼로그, Empty State — 를 디자인 시스템 폴더(또는 Option A 섹션)에 한 번씩 만들어 모든 화면이 재사용한다.

## User Goal
이후 REQ에서 새 화면을 만들 때 "흰 원형 아이콘 버튼 2개 헤더" 같은 패턴을 즉시 조립할 수 있고, radius·shadow·터치 타깃 44×44가 자동으로 지켜진다.

## Acceptance Criteria
- 다음 컴포넌트가 모두 존재: `IconButton`(흰 원형 44px), `Card`(흰 배경 radius 16~20 shadow y=2 blur=8 op=0.04), `FAB`(검정 56px), `BottomSheet`(상단 라운드 24px + 그립 핸들), `Toast`(알약형 1.5~2초), `ConfirmDialog`, `EmptyState`.
- 모든 인터랙티브 컴포넌트의 최소 터치 타깃이 44×44 (§1.6.7).
- 컬러는 REQ-001의 토큰 참조 (하드코딩 금지).
- Storybook 또는 단순 데모 페이지에서 모두 렌더 확인 (선택).

## Non-Goals
- 화면 단위 컴포넌트 (캘린더·에디터 등).
- 다크모드 — P1 (§8.2).

## Dependencies
REQ-001

## Suggested Scope
- Option B/C: `src/design-system/` 폴더에 컴포넌트당 파일 1개.
- Option A: HTML 안 `// ===== Components =====` 섹션에 함수/클래스 그룹.

## Expected Affected Areas
- Frontend: 디자인 시스템 폴더/섹션
- Backend: 없음
- Database: 없음
- Tests: 각 컴포넌트 렌더 + 핵심 prop 동작
- E2E: 없음

## Risk Level
Medium — 여기서 정한 prop 시그니처가 모든 화면에 굳음. 너무 좁게 만들면 REQ-007부터 우회 코드가 생김.

## Suggested Test Coverage
- 모든 컴포넌트가 정의된 토큰으로만 색·radius·shadow를 사용한다 (스타일 인스펙션).
- IconButton·FAB 클릭 시 onClick prop 호출.
- Toast가 지정된 ms 후 사라진다.

## Notes
CLAUDE.md "UI 컴포넌트 재사용 규칙" 항목 1·2의 출발점. 이 REQ 이후 모든 신규 UI는 먼저 이 폴더부터 확인해야 함.
