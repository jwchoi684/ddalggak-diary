# REQ-006: history-stack 기반 네비게이션 셸

## Status
DONE

## Completion Notes
첫 cycle에서 13개 phase 모두 PASS. Fix cycle 없음. Option C (Next.js App Router) 기반이라 React Router 미사용 — 파일 기반 라우팅으로 5개 page.tsx + not-found 구현. `Routes` 헬퍼 + barrel + 공유 `next/navigation` mock 헬퍼 (REQ-007/009/013/015~018가 재사용).

## Files Changed
- 신규(source, 7): `src/lib/navigation/{routes.ts (40L), index.ts (6L)}`, `src/app/{not-found.tsx (17L), diary/[date]/page.tsx (17L), list/page.tsx (8L), chat/page.tsx (8L), stats/page.tsx (8L)}`
- 신규(test, 5): `src/lib/navigation/__tests__/{setupNextNavigation.ts (31L), routes.test.ts (51L), setupNextNavigation.test.ts (29L)}`, `src/app/__tests__/{diary-date-page.test.tsx (62L), not-found.test.tsx (30L)}`
- 변경 없음: `package.json`, `next.config.ts`, 기존 source

## Tests Run
- `npm run typecheck` — PASS
- `npm run lint` — PASS
- `npm test` — PASS (151/151, 23 files: 131 baseline + 20 REQ-006)
- `npm run build` — PASS (6 routes: 5 static `○` + 1 dynamic `ƒ /diary/[date]`)
- `scripts/quality-gate.sh` — `QUALITY_GATE_PASS`

## Review Result
PASS — `.agent-state/09-code-review-report.md`. 3개 non-blocking 제안 + 2개 nits. Contract verbatim 일치, 모든 파일 ≤ 100 lines, no `as any`, no `"use client"` in shell.

## Security Result
PASS — `.agent-state/10-security-report.md`. 0 new findings. Path traversal in `/diary/[date]` correctly defended by Next.js + regex guard. Routes.diary encoding은 caller 책임으로 문서화 권장 (non-blocking nit).

## E2E Result
PASS — not applicable. Routing shell은 user interaction 없음. Playwright 부트스트랩은 REQ-007 (첫 user journey: FAB → /diary/[today])에서.

## Commit / PR
Not committed. 사용자 선택대로 REQ당 1 commit. `.agent-state/14-release-report.md`에 커밋 메시지 초안.

## Forward Constraints
- **REQ-007** (calendar):
  - Playwright 부트스트랩 + 첫 브라우저 E2E.
  - `/` 페이지가 client component 경계 가지면서 First Load JS 측정 (현재 baseline 103 kB).
  - `Routes.calendar` / `Routes.diary(today)` 사용 패턴 시작.
- **REQ-009** (editor):
  - 의미적 날짜 검증 (Feb 31 등) — REQ-006는 regex 형식만.
  - `/diary/[date]` 페이지가 localStorage 읽으면서 client component 될 가능성 — `ƒ` dynamic SSR 유지 vs `○` static 전환 재평가.
- **REQ-013** (list):
  - URL search params (`?month=YYYY-MM&sort=asc|desc`) read via `useSearchParams()`.
  - `Routes.listWithFilter()` 활용.
  - 값 검증 (잘못된 month/sort) — REQ-006 shell은 통과시킴.
- **REQ-015/017** (AI 채팅):
  - 인용 일기 칩 → `router.push(Routes.diary(date))` 패턴.
- **JSDoc nit** on `Routes.diary`: callers must pre-validate / encode raw user input. 다음 maintenance pass에서 추가.
- **setupNextNavigation.test.ts**: `beforeEach(resetNavigationMocks)` 누락. 케이스 추가 시 발견될 가능성.
- (Carry-forward) REQ-011 / REQ-019의 hard-gate 유지.

## Source
docs/requirements.md §2 (정보 구조), §2.1 (네비게이션 원칙), §11 (화면 흐름도)

## Summary
캘린더를 최상위로 두고, 모든 화면 간 이동을 브라우저 history-stack 기반으로 처리한다. 같은 에디터 화면이 캘린더·리스트·AI 채팅 인용 어디서 진입했는지에 따라 뒤로가기 목적지가 달라지며, 복귀 시 이전 화면의 스크롤 위치·선택된 달·정렬 상태가 보존된다.

## User Goal
사용자가 "내가 어디서 왔는지" 자연스럽게 돌아갈 수 있고, 다른 화면으로 우회해도 직전 컨텍스트가 사라지지 않는다.

## Acceptance Criteria
- 라우트 5개: `/`(캘린더), `/diary/:date`, `/list`, `/chat`, `/stats`. (Option A에서는 hash 또는 단일 state로 대응.)
- 리스트→에디터→뒤로 = **리스트로** 복귀 (캘린더 아님).
- AI 채팅 인용 일기→에디터→뒤로 = **AI 채팅으로** 복귀.
- 리스트 복귀 시 마지막으로 보고 있던 달·정렬 상태 유지.
- AI 채팅 복귀 시 같은 대화 위치(스크롤)로 복귀.
- 모달은 history-stack에 들어가지 않는다 — 모달 닫힘이 화면 뒤로가기로 이어지지 않음.

## Non-Goals
- 딥링크·공유 URL — v2.
- 브라우저 새로고침 후 상태 완전 복구 — 직전 화면 식별 정도만 권장(가능 범위).

## Dependencies
REQ-001

## Suggested Scope
- Option B/C: React Router 채택 + scroll restoration 훅 + 화면별 미니 상태 영속화.
- Option A: 단순 state machine + 진입 경로(referrer) 메모.

## Expected Affected Areas
- Frontend: 라우팅 셸, scroll-restore 훅, 화면 상태 메모 헬퍼
- Backend: 없음
- Database: 없음
- Tests: history 스택 시뮬레이션 (리스트→에디터→뒤로 = 리스트)
- E2E: 권장 — 3개 진입 경로 각각의 뒤로가기 동작

## Risk Level
Medium — 뒤로가기 동작은 사용자가 즉시 체감하는 부분이라 버그 시 신뢰도 손실 큼.

## Suggested Test Coverage
- 3개 경로별 에디터 진입 → 뒤로 → 올바른 화면 + 보존 상태.
- 모달 열고 닫기가 history에 영향을 주지 않음.

## Notes
CLAUDE.md "Navigation model" 섹션 그대로. Option B에서는 React Router가 history-stack을 거의 그대로 제공하므로 추가 작업이 적다.
